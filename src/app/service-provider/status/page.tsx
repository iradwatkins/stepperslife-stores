import ApplicationStatusClient from "./ApplicationStatusClient";

export const metadata = {
  title: "Application Status - SteppersLife Services",
  description: "Check the status of your service provider application.",
};

export default function ApplicationStatusPage() {
  return <ApplicationStatusClient />;
}
