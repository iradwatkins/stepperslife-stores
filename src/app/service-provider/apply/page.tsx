import ServiceProviderApplyClient from "./ServiceProviderApplyClient";

export const metadata = {
  title: "List Your Services - SteppersLife",
  description: "Join the SteppersLife service provider directory and connect with customers in the stepping community.",
};

export default function ServiceProviderApplyPage() {
  return <ServiceProviderApplyClient />;
}
